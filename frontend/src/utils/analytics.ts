export const trackEvent = (name: string) => {
  // if (window.posthog) {
  //   if (additionalData && Object.keys(additionalData).length > 0) {
  //     window.posthog.capture(name, additionalData);
  //   } else {
  //     window.posthog.capture(name);
  //   }
  // }
};